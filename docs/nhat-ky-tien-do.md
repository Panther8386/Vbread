# Nhật ký tiến độ

| Ngày | Giai đoạn | Đã làm | Còn lại / lưu ý |
|---|---|---|---|
| 16/09/2026 | Chuẩn bị | Tạo CLAUDE.md, tài liệu nghiệp vụ MVP, câu hỏi cần chốt, prompt mẫu | Trả lời docs/02-cau-hoi-can-chot.md |
| 17-18/09/2026 | Chuẩn bị | Lộ trình + khảo sát trên GitHub Pages; nhận diện thương hiệu (brand/); chốt mô hình đối tác, giá bán, in bill, phí nhượng quyền (GĐ-08 mới) | Còn thiếu: số ca/giờ ca, danh sách thiết bị bắt buộc, số tiền phí nhượng quyền/dịch vụ |
| 18/09/2026 | GĐ-00 | Dựng khung Next.js (App Router, TypeScript, Tailwind v4) + shadcn/ui; nạp màu/font thương hiệu vào globals.css; cài Vitest (test mẫu formatVnd) + Playwright (test mẫu homepage); `npm run lint` và `npm test` đều pass; `npm run build` thành công | Chưa làm: kết nối Supabase, migration, RLS, đăng nhập, PWA — đang chờ URL/anon key Supabase từ chủ dự án |
| 18/09/2026 | GĐ-00→GĐ-01 (nền tảng) | Kết nối Supabase thật; thêm vai trò `partner` (CLAUDE.md: owner/partner/manager/staff); 6 migration (`profiles` + trigger tự tạo hồ sơ, `carts`+`locations`, `manager_scopes`, `audit_logs`, grant quyền bảng cho `authenticated`/`service_role`, sửa lỗi đệ quy RLS profiles↔manager_scopes↔carts); đăng nhập số điện thoại + mật khẩu (dùng email giả `@vbread.local` phía sau vì Supabase Phone provider bắt buộc Twilio); tạo tài khoản `owner` đầu tiên, đăng nhập thử thành công; viết bộ test RLS thật (`src/lib/supabase/rls.test.ts`, 8 test: partner/manager/staff/anon đúng phạm vi) — phát hiện và sửa 1 lỗi đệ quy RLS nhờ test này; 3 script `scripts/*.mjs` (tạo tài khoản, đổi vai trò, dọn dữ liệu thử) | Còn thiếu 3 chi tiết ở docs/02 (số ca, thiết bị bắt buộc, số tiền phí); chưa làm Danh mục UI (GĐ-01), chưa làm PWA |
| 18/09/2026 | GĐ-00→GĐ-01 (nền tảng) | Thêm trang `/doi-mat-khau` (đổi mật khẩu khi đã đăng nhập, xác nhận lại mật khẩu cũ trước khi đổi); script `scripts/reset-password.mjs` cho owner đặt lại mật khẩu người khác khi họ quên (không tự khôi phục được vì không có email/SMS thật); test Playwright cho luồng đăng nhập sai mật khẩu | Chưa có UI cho owner gọi reset-password (đang là script tay) — có thể làm màn hình quản lý tài khoản ở GĐ-01 |
| 18/09/2026 | GĐ-01 | Màn hình Danh mục dưới `/danh-muc`: **Xe** (owner tạo + gán đối tác, đối tác sửa ghi chú/trạng thái xe mình), **Điểm bán** (owner CRUD), **Sản phẩm** (owner CRUD), **Giá bán** (owner thêm giá mới kèm ngày hiệu lực, giữ lịch sử, có trigger ghi `audit_logs` theo CLAUDE.md, view `current_prices` lấy giá hiện hành), **Tài khoản** (owner tạo mọi vai trò; đối tác tự tạo nhân viên/quản lý cho xe mình — Server Action `createAccount` kiểm quyền trong code trước khi dùng `service_role`, không dựa vào RLS). Thêm cột `profiles.created_by` để đối tác xem lại nhân viên mình vừa tạo. Mở rộng bộ test RLS lên 20 test (thêm sản phẩm/giá). Tạo dữ liệu mẫu: 2 xe, 3 điểm bán, 5 sản phẩm (BM01-03 + bánh nền + bao bì) kèm giá — `scripts/seed-sample-data.mjs` | Chưa test tự động cho quyền tạo tài khoản (đối tác chỉ tạo được cho xe mình) — mới kiểm bằng đọc lại code, chưa có test riêng; chưa làm PWA |
| 18/09/2026 | GĐ-01 (sửa theo phản hồi) | Sửa lỗi: tạo tài khoản Quản lý không chọn xe vẫn được — giờ bắt buộc chọn xe (nhiều xe) cho cả Quản lý và Đối tác, kiểm tra ở mọi người gọi (không chỉ riêng đối tác); thêm kiểm tra số điện thoại hợp lệ (`isValidVietnamesePhone`); owner sửa/vô hiệu hóa được tài khoản sau khi tạo (vô hiệu hóa = khóa đăng nhập thật qua `ban_duration`, không xóa dữ liệu); Xe/Điểm bán/Sản phẩm giờ owner sửa được đầy đủ trường (không chỉ bật/tắt); Giá bán cho sửa/xóa từng dòng (vẫn giữ lịch sử qua audit_logs). Gắn trigger `log_audit()` cho `profiles`, `carts`, `locations`, `products`, `manager_scopes` — đã kiểm tra thật qua API, audit_logs ghi đúng giá trị cũ/mới/người sửa. Test: 25/25 pass (`npm test`), `npm run build`/`lint` sạch | Vẫn chưa có test tự động riêng cho luồng tạo/sửa/xóa tài khoản (đã kiểm tay qua API) |
| 18/09/2026 | GĐ-01 (lỗi nghiêm trọng) | **Sự cố**: chủ dự án tự vô hiệu hóa được chính tài khoản owner đang dùng, mất quyền đăng nhập lại. Khắc phục ngay: script `scripts/unban-user.mjs` mở khóa (`ban_duration: "none"`) và kích hoạt lại (`status: "active"`) tài khoản — đã xác nhận đăng nhập lại được. Sửa gốc trong `setAccountStatus`: chặn tự vô hiệu hóa chính mình, chặn vô hiệu hóa owner cuối cùng còn hoạt động; ẩn luôn nút "Vô hiệu hóa" trên dòng tài khoản của chính người đang đăng nhập | Đã sửa và build qua, chưa commit trước khi làm tiếp yêu cầu tái cấu trúc route |
| 18/09/2026 | Điều hướng + Cấu hình hệ thống | Đăng nhập xong vào thẳng `/danh-muc` (không qua trang chủ); chuyển `/danh-muc/*` và trang đổi mật khẩu vào layout dùng chung `src/app/(app)/layout.tsx` (gộp menu, kiểm đăng nhập 1 nơi); thêm mục **Cấu hình hệ thống** (`/cau-hinh`) chứa "Đổi mật khẩu" (dời từ `/doi-mat-khau`); `/danh-muc` tự chuyển sang trang đầu tiên đúng quyền của từng vai trò. Test 25/25 pass, lint/build sạch | Đã làm tiếp: xem mục ngay dưới |
| 18/09/2026 | Nhật ký thay đổi (audit_logs) | Thêm trang `/cau-hinh/nhat-ky` (chỉ owner xem) hiển thị 200 thay đổi gần nhất: bảng nào, thêm/sửa/xóa, ai sửa, lúc nào (giờ Việt Nam), trường nào đổi từ giá trị gì sang giá trị gì, lý do. Viết hàm `diffAuditValues()` (`src/lib/audit.ts`) và `formatDateTimeVn()` (`src/lib/date.ts`) kèm test riêng. Test: 32/32 pass, lint/build sạch (gặp 2-3 lần lỗi mạng chập chờn khi gọi Supabase lúc chạy test — chạy lại là qua, không phải lỗi code) | Chưa có bộ lọc theo bảng/người sửa trên UI (đang xem tuần tự 200 dòng gần nhất) — có thể thêm nếu chủ dự án thấy cần |
| 19/09/2026 | GĐ-02 (phần 1) — Cấu hình ca & Phân công ca | Chốt 3 quyết định còn thiếu với chủ dự án: số ca/giờ ca do owner tự cấu hình (không cố định cứng); chỉ đối tác/chủ xe phân công ca (quản lý chỉ xem); bàn giao giữa ca để dành cho phần "mở ca/đóng ca" sau. 3 bảng mới (`shift_templates`, `shifts`, `shift_staff`) + RLS theo đúng khuôn owner/partner/manager đã có, thêm hàm `is_staff_of_shift()`/`get_shift_cart()` tránh đệ quy, gắn `log_audit()`. Màn hình `/cau-hinh/ca` (owner cấu hình ca mẫu: tên, giờ bắt đầu/kết thúc) và `/ca-ban` (xem/phân công ca theo ngày — RLS tự giới hạn đúng phạm vi từng vai trò, không cần lọc thêm trong code; chặn đổi điểm bán trong ngày; hủy ca thay vì xóa cứng). Thêm hàm `businessDateVn()` (giờ kinh doanh theo giờ VN) kèm test. Mở rộng RLS test lên 40/40 pass (partner tạo/xem ca xe mình, manager đọc được nhưng không tạo được ca, staff thấy đúng ca + tên xe của mình). Migration áp dụng qua Supabase SQL Editor (không qua CLI) — phát hiện thiếu 1 policy khi test lần đầu, đã bổ sung và chạy lại. `npm run lint`/`test`/`build` đều sạch | Phần 2 (mở ca với hàng+tiền đầu ca, bàn giao, đóng ca) cần bảng `stock_movements` — sẽ lập kế hoạch riêng khi làm mục "Hàng hóa" |
| 19/09/2026 | Sửa dữ liệu + quy tắc trạng thái xe | Xóa tài khoản test "Test2" (số điện thoại không hợp lệ, tạo trước khi có kiểm tra số điện thoại — không phải lỗi đang xảy ra, chỉ là dữ liệu test cũ sót lại). Thêm quy tắc: xe chưa gán đối tác **và** chưa từng có ca nào thì không được chuyển sang "Đang hoạt động" (kiểm tra trong `updateCartFull`, báo lỗi rõ ràng qua `useActionState` — tách form sửa xe của owner ra `cart-row.tsx`); xe mới tạo giờ mặc định "Chưa hoạt động" thay vì "Đang hoạt động". Đã chỉnh lại 2 xe VB-01/VB-02 đang vi phạm quy tắc mới về đúng trạng thái. Test 40/40 pass, lint/build sạch | Không có |
| 19/09/2026 | Nhật ký thay đổi — phân trang + bộ lọc | Trang `/cau-hinh/nhat-ky`: thêm phân trang (30 dòng/trang, dùng `range()` + đếm tổng số dòng khớp) thay vì giới hạn cứng 200 dòng; thêm bộ lọc theo Bảng và theo Người sửa (dùng `<form method="get">` + link phân trang giữ nguyên bộ lọc, không cần JavaScript phía client). Thêm nhãn cho 3 bảng mới của GĐ-02 (`shift_templates`, `shifts`, `shift_staff`) vào danh sách hiển thị. Lint/build sạch | Không có |
| 19/09/2026 | Nhật ký thay đổi — chọn số dòng/trang + bấm số trang | Thêm chọn 10/15/25/50/100 dòng/trang (mặc định 25); thay nút Trước/Sau bằng dãy số trang bấm được thẳng tới trang đó (có "..." khi nhiều trang) | Không có |
| 19/09/2026 | Rà soát quy trình khởi tạo (vòng 1) | Chủ dự án yêu cầu rà soát logic + thứ tự: xe → đối tác/quản lý/nhân viên → cấu hình ca → phân công ca. Sửa: gán đối tác xong xe tự động "Đang hoạt động". Viết `docs/quy-trinh-khoi-tao.md`. **Vòng 1 này sau đó bị chủ dự án chỉnh lại ở vòng 2 (xem dòng dưới)** | Đã sửa lại theo vòng 2 |
| 19/09/2026 | Rà soát quy trình khởi tạo (vòng 2 — chốt) | Chủ dự án mô tả lại chi tiết quy trình thực tế, khác vòng 1 ở 3 điểm — đã xác nhận qua 4 câu hỏi: (1) **Quản lý chỉ owner tạo được** (không phải đối tác thuê — quản lý phục vụ chăm sóc đối tác & cung ứng sản phẩm, do owner phân bổ vào xe); ẩn lựa chọn "Quản lý" khỏi form khi người tạo là đối tác. (2) Giữ nguyên: Nhân viên vẫn do cả Owner và Đối tác tự tạo được. (3) Giữ nguyên: chỉ Đối tác/Owner phân công ca, Quản lý chỉ xem. (4) **Đảo ngược lại vòng 1**: xe KHÔNG tự active khi gán đối tác nữa — chỉ chuyển "Đang hoạt động" khi có **nhân viên được phân công vào 1 ca cụ thể** (`createShift` tự cập nhật `carts.status` sau khi gán nhân viên thành công). Sửa kèm: form Phân công ca trước đây chỉ cho chọn xe "Đang hoạt động" — nếu giữ vậy sẽ bế tắc (xe không active thì không chọn được để tạo ca đầu tiên) — bỏ lọc trạng thái, cho chọn mọi xe. Cập nhật CLAUDE.md, docs/01, docs/quy-trinh-khoi-tao.md theo đúng logic mới. Test 40/40 pass, lint/build sạch, đã kiểm tra bằng dữ liệu thật đúng luồng (xe inactive → gán đối tác vẫn inactive → gán nhân viên vào ca → active) | Không có |
| 19/09/2026 | GĐ-02 (phần 2a) — Mở ca | Bước tiếp theo trong "Ca bán" là mở ca/bàn giao/đóng ca, nhưng đóng ca + đối soát tiền cần dữ liệu doanh thu từ "Bán hàng" (chưa xây) nên chỉ làm phần tự đứng được ngay: **mở ca**. Bảng mới `stock_movements` (loại nhận/bán/trả/hủy/hao hụt/kiểm kê — bước này chỉ dùng "nhận", các loại còn lại để dành cho phần Hàng hóa sau); mở rộng `shifts` thêm `opening_cash`, `opened_at`, `opened_by`, trạng thái thêm `open`. Trang chi tiết ca `/ca-ban/[shiftId]`: nhân viên được phân công (hoặc đối tác/owner) nhập số lượng hàng nhận đầu ca theo từng sản phẩm + tiền lẻ đầu ca, bấm "Mở ca". Cố ý bỏ qua: chụp ảnh khi nhận hàng (cần thêm Supabase Storage, để sau, không chặn tiến độ). Test RLS mở rộng lên 44/44 pass (nhân viên trong ca ghi được phiếu kho, nhân viên khác/partner khác bị chặn). Đã kiểm tra bằng dữ liệu thật (tạo ca → mở ca → xác nhận `stock_movements` + `shifts.status` + có ghi `audit_logs`). Lint/build sạch | Tiếp theo: Bán hàng (GĐ-03), rồi mới quay lại Hàng hóa trong ca + Đóng ca + Đối soát tiền + Bàn giao + Duyệt |
| 19/09/2026 | GĐ-03 — Bán hàng | 3 bảng mới `sales`/`sale_items`/`payments` (giá chốt tại thời điểm bán, không phụ thuộc `prices` đổi sau); `stock_movements` thêm cột `sale_id` để hoàn tồn khi hủy đơn (đã bổ sung thêm policy + grant `delete` còn thiếu từ trước). Hàm `calcSubtotal`/`calcSaleTotal` (`src/lib/sales.ts`, có test) dùng chung cho client (xem trước) và server (tính lại, không tin số client gửi). Màn `/ca-ban/[shiftId]/ban-hang`: lưới sản phẩm chạm 1 lần thêm vào giỏ (đúng quy tắc "không quá 2 chạm"), giỏ hàng chỉnh số lượng, giảm giá, chọn phương thức thanh toán, "Thanh toán" → tạo đơn + tự trừ tồn kho. Trang hóa đơn `/ca-ban/[shiftId]/ban-hang/[saleId]`: xem/in (qua `window.print()`, CSS ẩn menu khi in), hủy đơn (bắt buộc ghi lý do, tự hoàn tồn kho). **Đã xác nhận với chủ dự án**: "in bill" ở mức xem/in qua trình duyệt, chưa nối hóa đơn điện tử thật (cần thông tin nhà cung cấp hóa đơn điện tử + mã số thuế, làm sau khi có). Bỏ qua: sửa đơn tại chỗ, chia thanh toán nhiều phương thức, cảnh báo tồn âm. Test RLS mở rộng lên 53/53 pass. Đã kiểm tra bằng dữ liệu thật (tạo đơn → trừ tồn → hủy đơn → hoàn tồn → audit_logs ghi đủ). Lint/build sạch | Tiếp theo: quay lại làm trọn Hàng hóa trong ca (nhận thêm/trả/hủy/hao hụt/kiểm kê cuối ca) + Đóng ca + Đối soát tiền + Bàn giao + Duyệt |
